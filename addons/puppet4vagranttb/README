HOWTO

1/ Launch the script addons/puppet4vagranttb/vbox_handler.sh

2/ Connect into the kadeploy_master VM
$> cd $KADEPLOY_ROOT_DIR
$> CLUSTER_SIZE=2 vagrant up
$> vagrant ssh kadeploy_master

3/ Become root and try to deploy
$> sudo su -
$> kadeploy3 -e wheezy-min -m node-[1,2]
